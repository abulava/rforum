<h2>Edit the topic</h2>
<%= form_for @topic do |f| %>
  <%= render 'shared/form_messages', :object => f.object %>

  <%= f.label :title %> <i>(3 chars minimum, 140 chars maximum)</i><br />
  <%= f.text_field :title %>
  <div class="actions">
    <%= f.submit "Submit" %>
  </div>
<% end %>
